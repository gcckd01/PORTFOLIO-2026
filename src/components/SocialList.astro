---
import { Icon } from "astro-icon/components";

/**
	Uses https://www.astroicon.dev/getting-started/
	Only installed pack is: @iconify-json/mdi
*/
const socialLinks: {
	friendlyName: string;
	isWebmention?: boolean;
	link: string;
	name: string;
	download?: boolean;
}[] = [
	{
		friendlyName: "Github",
		// Github profile for Ayush Kedia
		link: "https://github.com/gcckd01", //
		name: "mdi:github",
	},
	{
		friendlyName: "LinkedIn",
		// LinkedIn profile for Ayush Kedia
		link: "https://www.linkedin.com/in/ayush-kedia-92813824a/",//
		name: "mdi:linkedin",
	},
	{
		friendlyName: "Download Resume",
		// Download link for Ayush Kedia's resume
		link: "/Ayush-Kedia-2026-Resume.pdf", //
		name: "mdi:file-document-outline",
		download: true,
	},
];
---

<div class="flex flex-wrap items-end gap-x-2">
	<p>Find me on</p>
	<ul class="flex flex-1 items-center gap-x-2 sm:flex-initial">
		{
			socialLinks.map(({ friendlyName, isWebmention, link, name, download }) => (
				<li class="flex">
					<a
						class="hover:text-link inline-block"
						href={link}
						rel={`noreferrer ${isWebmention ? "me authn" : ""}`}
						target={download ? "_self" : "_blank"}
						download={download ? "Ayush_Kedia_Resume.pdf" : undefined}
					>
						<Icon aria-hidden="true" class="h-8 w-8" focusable="false" name={name} />
						<span class="sr-only">{friendlyName}</span>
					</a>
				</li>
			))
		}
	</ul>
</div>
